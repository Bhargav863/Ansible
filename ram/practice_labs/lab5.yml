---
- name: Create a playbook that will run only on localhost and displays all the even numbers that are between 20 and 40 inclusive
  hosts: localhost
  tasks:
    - name: Display all even numbers between 20 and 40
      debug:
        msg: "{{ item }}"
      loop: "{{ range(20,41,2) | list }}"


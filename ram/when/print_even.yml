---
- name: This example demonstrates the usage of when with loops
  hosts: localhost
  vars:
    num: [1,2,3,4,5,6,7,8,9]
  tasks:
    - name: Print even n is an even number"
      debug:
        msg: "{{ item }}  is an even number"
      loop: "{{ num }}"
      when: item%2 == 0

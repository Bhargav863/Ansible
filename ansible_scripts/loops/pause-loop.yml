---
- name: Pausing over loops
  hosts: localhost
  tasks:
    - name: Ten seconds countdown
      debug:
        msg: "{{ 10-item}} seconds remaining "
      loop: "{{ range(10) | list }}"
      loop_control:
         pause: 1
    - name: Display happy birthday
      debug:
        msg: Happy Birthday
